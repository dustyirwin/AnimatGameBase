
"""
## BASIC AniMage GAME SETTINGS
#### Edit the elements below to modify basic game options.
"""

# length in pixels
const SCREEN_WIDTH = Int32(1920)

# length in pixels
const SCREEN_HEIGHT = Int32(1080)

# length in pixels
const SCREEN_BORDER = Int32(10)

# max FPS
const MAX_FPS = Int32(30)

# dict
const GAME_SETTINGS = Dict(
    :SCREEN_HEIGHT => SCREEN_HEIGHT,
    :SCREEN_WIDTH => SCREEN_WIDTH,
    :SCREEN_BORDER => SCREEN_BORDER,
    :MAX_FPS => MAX_FPS,
)